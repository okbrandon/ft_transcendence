import React, { useState } from 'react';

const Chat = () => {

	return (
		<header className='msg-window-header'>
			<div className='msg-window-container'></div>
		</header>
	);
};

export default Chat;